% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/labels.R
\name{aib_color_title}
\alias{aib_color_title}
\title{Colored Title or Subtitle}
\usage{
aib_color_title(text, colors, element = c("title", "subtitle"))
}
\arguments{
\item{text}{A single character string for the title or subtitle.}

\item{colors}{A named character vector mapping substrings to colors
(e.g., \code{c("Group A" = "#1B3E6F")}). Each name found in \code{text} is
wrapped in a colored HTML span.}

\item{element}{Which text element to set: \code{"title"} or \code{"subtitle"}.}
}
\value{
A list of ggplot2 components that can be added to a plot
with \code{+}. Because this includes a partial \code{\link[ggplot2:theme]{ggplot2::theme()}}
modification, add it \strong{after} your base theme (e.g., \code{theme_aib()})
in the \code{+} chain.
}
\description{
Creates a plot title or subtitle with specific words or phrases rendered
in color. Designed as a companion to \code{\link[=aib_direct_label]{aib_direct_label()}} so that group
names in the title match their line colors, replacing the need for a
legend.
}
\examples{
library(ggplot2)
df <- data.frame(
  year = rep(2020:2024, 2),
  value = c(1:5, 5:1),
  group = rep(c("A", "B"), each = 5)
)
ggplot(df, aes(year, value, color = group)) +
  geom_line() +
  scale_color_aib() +
  theme_aib() +
  aib_color_title(
    "A outperformed B over time",
    colors = c("A" = "#1B3E6F", "B" = "#C00404")
  )

}
