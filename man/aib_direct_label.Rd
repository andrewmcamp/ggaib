% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/labels.R
\name{aib_direct_label}
\alias{aib_direct_label}
\title{Direct Labels on a Secondary Y-Axis}
\usage{
aib_direct_label(
  data,
  x,
  y,
  group,
  colors = NULL,
  adjust = NULL,
  size = NULL,
  bold = FALSE,
  ...
)
}
\arguments{
\item{data}{Data frame used in the plot.}

\item{x, y, group}{String column names for the x-axis, y-axis, and
grouping variable.}

\item{colors}{Optional named character vector mapping group names to
colors (e.g., \code{c("Group A" = "#1B3E6F")}). Defaults to
\code{\link[=aib_colors]{aib_colors()}} in palette order, matching \code{\link[=scale_color_aib]{scale_color_aib()}}.}

\item{adjust}{Optional named numeric vector of vertical adjustments
to prevent label overlap (e.g., \code{c("Group A" = 0.5)}). Groups not
listed receive no adjustment.}

\item{size}{Font size for the labels in points. Defaults to \code{NULL},
which uses the theme's default axis text size.}

\item{bold}{Logical; if \code{TRUE}, labels are rendered in bold.
Defaults to \code{FALSE}.}

\item{...}{Additional arguments passed to \code{\link[ggplot2:scale_continuous]{ggplot2::scale_y_continuous()}}
(e.g., \code{limits}, \code{breaks}, \code{labels}).}
}
\value{
A list of ggplot2 components that can be added to a plot
with \code{+}. Because this includes a partial \code{\link[ggplot2:theme]{ggplot2::theme()}}
modification, add it \strong{after} your base theme (e.g., \code{theme_aib()})
in the \code{+} chain.
}
\description{
Replaces the legend with group labels placed on a secondary y-axis,
aligned with each group's final value. Label colors match the group
colors for easy identification.
}
\details{
Because this function sets \code{\link[ggplot2:scale_continuous]{ggplot2::scale_y_continuous()}} with a
secondary axis, it should be used instead of (not alongside) a
separate \code{scale_y_continuous()} call.
}
\examples{
library(ggplot2)
df <- data.frame(
  year = rep(2020:2024, 2),
  value = c(1:5, 5:1),
  group = rep(c("A", "B"), each = 5)
)
ggplot(df, aes(year, value, color = group)) +
  geom_line() +
  scale_color_aib() +
  theme_aib() +
  aib_direct_label(df, "year", "value", "group")

}
